{
  "name": "lightning-admin",
  "version": "0.1.0",
  "description": "Operator desktop for Lightning Mesh — Winbox-style local discovery and admin",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "tauri": "tauri"
  },
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.70.3",
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "@tauri-apps/cli": "^2",
    "jsdom": "^29.1.1",
    "svelte": "^5.57.0",
    "svelte-check": "^4.6.0",
    "typescript": "~5.9.2",
    "vite": "^8.0.16",
    "vitest": "^4.1.8"
  }
}
