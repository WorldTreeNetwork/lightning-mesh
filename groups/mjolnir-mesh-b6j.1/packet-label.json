{
  "schema_version": "1.0",
  "id": "pkt-b6j-1-label",
  "node_id": "mjolnir-mesh-b6j.1",
  "capability": "mesh-https-names",
  "requester": {
    "id": "grok-conductor",
    "kind": "model",
    "harness": "grok",
    "signing": {
      "mode": "stand-in",
      "stand_in_id": "grok-conductor"
    }
  },
  "assignee": {
    "id": "sol-implement",
    "kind": "model",
    "harness": "codex",
    "interface": "gpt-5.6-sol",
    "signing": {
      "mode": "stand-in",
      "stand_in_id": "sol-implement"
    }
  },
  "role": "worker",
  "goal": "First act slice of add-https-aliases: shared HTTPS key-label function. blake3 of the 32-byte Ed25519 owner pubkey, take first 80 bits (10 bytes), RFC 4648 base32 lowercase no padding alphabet a-z2-7, 16 characters. Same vectors in Rust (mjolnir-mesh library, default features, no iroh) and TypeScript (hello-mesh-web, @noble/hashes). mjolnir-hello already depends on mjolnir-mesh default features so it inherits the Rust fn — export it from lib.rs. Shared fixture JSON both tests load. Include empty-key and known-vector cases plus round-trip length=16 and charset. Check the label-function task box. Do not implement AliasTable, ACME, TLS, or DNS forwarding. CARGO_TARGET_DIR=/tmp/lm-target. bun test for the TS spec.",
  "anchors": [
    {
      "symbol": "openspec/changes/add-https-aliases/design.md:25",
      "invariant": "80-bit blake3, RFC 4648 base32 lowercase no padding, 16 chars."
    },
    {
      "symbol": "openspec/changes/add-https-aliases/reviews/2026-09-15-advise-r3.md:1",
      "invariant": "Advise accept. Collision fail-closed is later; this slice is the label function only."
    },
    {
      "symbol": "crates/mjolnir-hello/Cargo.toml:24",
      "invariant": "hello uses mjolnir-mesh default features only; do not enable daemon/iroh."
    }
  ],
  "acceptance": {
    "kind": "command",
    "command": "CARGO_TARGET_DIR=/tmp/lm-target cargo test -p mjolnir-mesh https_label --offline && cd hello-mesh-web && bun run test",
    "criteria": [
      "Rust and TS produce identical 16-char labels for the shared fixtures",
      "Charset is a-z2-7 only",
      "No AliasTable/ACME/TLS/DNS forwarding"
    ]
  },
  "out_of_scope": [
    {
      "item": "AliasTable, issuance, ACME adapter, router TLS",
      "tracked_as": "mjolnir-mesh-b6j.1 remaining tasks"
    }
  ],
  "constraints": {
    "permission": "write",
    "paths": [
      "crates/mjolnir-mesh/src/lib.rs",
      "crates/mjolnir-mesh/src/https_label.rs",
      "crates/mjolnir-mesh/Cargo.toml",
      "Cargo.lock",
      "hello-mesh-web/src/lib/https/label.ts",
      "hello-mesh-web/src/lib/https/label.spec.ts",
      "hello-mesh-web/src/lib/https/fixtures/labels.json",
      "hello-mesh-web/package.json",
      "openspec/changes/add-https-aliases/tasks.md"
    ],
    "do_not": [
      "deploy",
      "push",
      "fold",
      "enable iroh on mjolnir-hello",
      "implement AliasTable or TLS"
    ]
  },
  "load_class": "intention-critical",
  "rigor": "architecture",
  "density": "standard",
  "surface": "packet-only"
}
