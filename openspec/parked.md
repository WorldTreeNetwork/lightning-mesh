# Parked

> **PARKED** — register of parks that are not in-flight OpenSpec changes.
> In-flight `PARKED` banners are listed by `ready.py` automatically.
> Add a row here when you park something that has no `openspec/changes/<id>/`.

| id | kind | revive | where |
|---|---|---|---|
| — | — | — | — |
