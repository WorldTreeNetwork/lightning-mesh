# Tasks

- [x] Human activation — Duke `activate all` 2026-09-18
- [x] vwifi-server on host; vwifi-client in guests; two hwsim radios
- [x] 802.11s ESTAB a↔b; client AP (`wlan-ap`) up
- [x] `vwifi-ctrl set` distance: inactive time climbs (hwsim RSSI often stays -10 dBm)
- [x] Partition vwifi: SSH on 10.99 lives; mesh inactive >> beacon. Overlay/babel check is `sim.5` (meshd not installed yet)

Not owed: install-node UCI (`sim.5`), roam STA (`sim.7`).
