# Work tracking

`mjolnir-mesh-ai0.1.1` is the canonical codec/holder-proof slice. Its completion
does not close ai0.1 or fold this change; the authorization engine, trusted time
and integration contracts remain owed. The codec-profile pin needs fresh review.

Bead mjolnir-mesh-ai0.1 owns profile design, instrument review, red-first vectors,
implementation and focused verification. The bead and its dependency edges are
authoritative; no duplicate markdown task list is maintained. No checkboxes does
not mean no work. Do not fold without implementation and test evidence.
