# Tasks

- [x] `openspec/changes/add-mjolnir-settings-store` proposal and deltas
- [x] Close 849.1 with the AD (store is canonical; apply is the only UCI projector)
- [x] `config radio` + `config wan_admin` in mjolnir UCI template
- [x] apply: ensure sections; env writes store; store exports into setup-wireless
- [x] `mjolnir-wan-admin` reads `mjolnir.wan_admin.timeout`
- [x] `sh -n` on touched scripts

Not owed here:

- Operator UI (`849.3`)
- Admin desktop (`st1.3`)
- Fold
