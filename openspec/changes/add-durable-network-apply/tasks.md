# Work tracking

`mjolnir-mesh-ai0.5` owns this instrument change. Beads holds work and findings;
this is a pointer, not a duplicate task list. No checkboxes does not mean fold-ready.

Implementation owes storage/state-machine tests, OpenWrt adapter/shared-lock/boot
integration and focused fault-injection proof. Hardware qualification remains
`mjolnir-mesh-lpv` / `mjolnir-mesh-z3th`; independent review is required before act.

