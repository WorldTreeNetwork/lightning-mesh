# Tasks

- [x] Parse the kernel route table and expose whether a usable default route exists.
- [x] Make CAPPORT and intercepted OS probes conditional on that route state.
- [x] Add the self-contained offline mesh page and focused state-transition tests.
- [x] Run the `mjolnir-hello` test suite and production clippy.
- [ ] Cross-build the OpenWrt binary from the committed tree.
- [ ] Fold the behavior into the living spec and archive this change.
