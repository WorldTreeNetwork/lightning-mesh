# Learnings

Append-only. One line per hard-won fact. Dated, with a file reference.

- 2026-09-16 — m3000-b STA/WWAN to *Symbio* (`wireless.uplink` + `network.wwan` metric 20) taken down; radio0 is the Lightning Mesh client AP again (ch 6). Default now babel via `10.254.61.115` (tr3000). setup-wireless.sh still refuses while a STA section exists (z3th). (`wireless.uplink` deleted on-box)
- 2026-09-15 — Household nodes on one ethernet switch merge client L2: indoor-b AP DHCP-vended m3000's `10.42.242.x`; ping to `.1` 2s ttl=63; iOS spinner/no lease (proxy-ARP fails DAD). `reconcile_client_uci` must not re-add `192.168.1.1/24` (659 alias; AP3000 notes already forbade it). Dataplane detaches copper from `br-lan` after two live foreign `10.42.y.1` neighs. Recovery is `fe80` + `10.42.x.1`, not a fleet-identical alias. (`crates/mjolnir-mesh/src/heal.rs`, meshd `reconcile_client_dataplane`)
- 2026-09-15 — Cudy M3000 front LED is bicolor red/white (`red:wan-online`, `white:wan-online`). `green:wan`/`green:lan` are port lamps, not topology. 849.5 no-ops without `amber:status`. (`crates/mjolnir-mesh/src/led.rs`, mjolnir-mesh-849.6)
- 2026-09-15 — LED SKU probes (849.7): TR3000 `red:power`+`white:status` (same mix as M3000); AP3000 Outdoor `red:power`+`green:status` (must beat TR3000 — both have `red:power`); WR3000S all-white which-lamp (`white:status` / `wan-online` / `wlan-5ghz`). Never write `white:wps` — wan-admin owns it. (`crates/mjolnir-mesh/src/led.rs`)
- 2026-09-15 — aarch64 musl `mjolnir-hello` grew +1.50 MiB (+119.5%) after ncy.1 mixed embed+TLS (`ureq` 2.12 + `rustls` 0.23): before `5654ce5` 1.26 MiB, after `4606896` tree 2.76 MiB. The delta is not TLS-only (older `static/` embed in the before binary). It does not authorize dropping to plain-HTTP fetch — that contradicts the self-signed-https scenario and needs a change amendment. (`groups/mjolnir-mesh-ncy.1/size-delta.md`, `add-mini-app-contract`)
- 2026-09-14 — Uplink-aware portal behavior supersedes the earlier default-open rule: the kernel default route is the local data-plane truth. Route present means `captive:false` plus exact OS success; no route means an informational offline portal. User action never changes either state. (`crates/mjolnir-hello/src/portal.rs`, `openspec/specs/captive-portal/spec.md`)
- 2026-09-14 — Default-open client behavior supersedes the 2026-08-31 captive-sheet buttons: CAPPORT always reports `captive:false`, intercepted OS probes receive their byte-exact success payload immediately, and `/api/portal/pass` is compatibility-only. (`crates/mjolnir-hello/src/routes.rs`, `openspec/specs/captive-portal/spec.md`)
- 2026-08-31 — This repo had no `openspec/`; `ready.py` / `run.py --until fold` stop empty until a change is actually scaffolded. (`openspec/`, `/Users/dukejones/.agents/skills/ready/scripts/ready.py`)
- 2026-08-31 — Captive portal copy is two buttons: IdentiKey CTA + “Just the internet, please”. There is no “No thanks” — that was the same pass-through. Direct fix, not a foldable change. (`crates/mjolnir-hello/src/portal.rs`)
- 2026-08-31 — Factory client SSID ⚡ and live-fleet `Lightning Mesh` must stay two knobs. Changing the `setup-wireless.sh` default does not rename the air until `mjolnir-apply` `RUN_WIRELESS=1` with that env. Keep `wireless.env.example` as the live override. (`deploy/openwrt/setup-wireless.sh`, `add-client-network-name`)
- 2026-09-04 — Lightning Admin must wrap `update-fleet.sh --wireless`; discovery still has no SSH. Admin is another writer of `mjolnir-apply`, not a live UCI mutation. Field apply on live nodes was not exercised this session. (`admin/`, `add-lightning-admin-network-name`)
- 2026-09-04 — Do not wrap Admin network-name in `update-fleet.sh`: local `mjolnir-meshd-aarch64` was 2026-08-07 and would downgrade live boxes; overlay `10.254.x` is unreachable from the house WAN LAN; all-skipped must not be OK. Wireless-only `apply-network-name.sh` + `LIGHTNING_FLEET_SSH`. wr3000s-a WPS window dies on `fw4 reload`/`wifi reload` (no Allow-SSH-WAN); ap3000 survived via leftover UCI Allow-SSH-WAN. (`deploy/openwrt/apply-network-name.sh`, ap3000 OK idempotent Lightning Mesh)
- 2026-09-04 — wr3000s-a wireless-only apply OK (SSID Lightning Mesh, binary unchanged) but br-mesh had no 10.254 until meshd restart; health gate passed on hello HTTP not overlay. ap3000 WAN SSH survived via leftover UCI Allow-SSH-WAN; wr3000s-a WPS window died on fw4/wifi reload. (`lq1`-adjacent)
- 2026-09-10 — After `fw4 reload` the WPS WAN SSH nft rule is gone (`mjolnir-wan-admin status` disarmed) but the sleeper and blinking WPS LED can run out the rest of the timeout; next press re-arms. Failure is closed; the lamp is not authoritative. Leftover bead `mjolnir-mesh-nin`, not this fold. (`deploy/openwrt/files/usr/sbin/mjolnir-wan-admin`, advise `add-wps-wan-admin`)
- 2026-09-10 — Coordinate stamps: subject may differ from stamper (phone stamps a router). Do not copy node_name's `node_id == stamper` drop. Book is `node_id` → i32 e7, LWW on unix `stamped_at` (stamper lex tie-break); gossip `CoordinateAnnounce` verifies no signatures; unmarked omits JSON keys. (`crates/mjolnir-mesh/src/crdt/coordinate.rs`, `add-node-coordinates`)
- 2026-09-10 — Phone stamp ingress: `POST /api/coordinate-stamp` on hello verifies Ed25519 over e7 integers (`mjolnir-coordinate-stamp:v1`), spools `coordinates/{stamper}.json`; meshd sweep applies LWW and gossips without re-verify. Unknown node / bad sig / missing lat-lon write nothing. (`crates/mjolnir-hello/src/routes.rs`, `add-compass-node-mark`)
- 2026-09-10 — add-coverage-sweep first slice painted fixture walks on web3d-space `/mesh` (unknown/thin/covered + completeness). lightning-mesh owed no sample crate this fold; tasks were checked here, paint lives in the sibling visualizer. (`web3d-space` 324d416, `add-coverage-sweep`)
- 2026-09-11 — `GET /api/directory` (hello.mesh) reads `directory.json`, which was projected from the **v1** `ServiceBook`. `meshd publish` writes **v2** (`services2.state`). DNS answered `cam0.6kwh.mesh`; Services stayed empty. File projection now uses v2. (`crates/mjolnir-mesh/src/bin/mjolnir-meshd.rs` `write_directory_projection`)
- 2026-09-11 — IPv6 unparked as a parallel plane, not a spine (`mjolnir-mesh-v6`). If it has an identity, ULA is hash(id) — never allocate. SLAAC+RA replace any IPv6 lease CRDT / DHCPv6; do not wire `LeaseEntry` for v6. Apps self-assign ULA and dial iroh by node-id (no router hop, no sz9). Phones still DHCP v4. (`docs/network-coordination/ipv6-parallel-plane.md`)
- 2026-09-11 — `build_directory_snapshot_v2_projects_published_device` was missing `coordinate_book`; compile-fixed while adding `link_local_*` to `DirectoryNode`. (`crates/mjolnir-mesh/src/bin/mjolnir-meshd.rs`, `add-link-local-mgmt`)
- 2026-09-11 — Identity ULA add is hash-derived (`ula_addr`); `IFA_F_NODAD` netlink flag is not set. Log says nodad-equivalent. (`crates/mjolnir-mesh/src/bin/mjolnir-meshd.rs` `assign_ula_addr`, `add-identity-derived-ula`)
